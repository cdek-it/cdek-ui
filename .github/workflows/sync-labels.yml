name: 🔄 Sync Labels to Satellites

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths: ['.github/labels-config.json']

jobs:
  # Синхронизация меток в Hub репозитории
  sync-hub-labels:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Hub
        uses: actions/checkout@v4
        
      - name: Sync labels in Hub repository
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            
            if (!fs.existsSync('./.github/labels-config.json')) {
              console.log('❌ labels-config.json not found');
              return;
            }
            
            const config = JSON.parse(fs.readFileSync('./.github/labels-config.json', 'utf8'));
            
            console.log('🔄 Syncing labels in Hub repository...');
            
            // Получаем все метки из конфига
            const allLabels = [];
            for (const category of Object.values(config.labels)) {
              if (Array.isArray(category)) {
                allLabels.push(...category);
              }
            }
            
            console.log(`📋 Processing ${allLabels.length} labels...`);
            
            // Получаем существующие метки в Hub
            const existingLabels = await github.paginate(
              github.rest.issues.listLabelsForRepo,
              {
                owner: context.repo.owner,
                repo: context.repo.repo,
                per_page: 100
              }
            );
            
            const existingLabelNames = existingLabels.map(label => label.name);
            
            // Создаем или обновляем метки в Hub
            let created = 0;
            let updated = 0;
            
            for (const labelDef of allLabels) {
              try {
                if (existingLabelNames.includes(labelDef.name)) {
                  await github.rest.issues.updateLabel({
                    owner: context.repo.owner,
                    repo: context.repo.repo,
                    name: labelDef.name,
                    color: labelDef.color,
                    description: labelDef.description || ''
                  });
                  updated++;
                  console.log(`🔁 Updated: ${labelDef.name}`);
                } else {
                  await github.rest.issues.createLabel({
                    owner: context.repo.owner,
                    repo: context.repo.repo,
                    name: labelDef.name,
                    color: labelDef.color,
                    description: labelDef.description || ''
                  });
                  created++;
                  console.log(`✅ Created: ${labelDef.name}`);
                }
              } catch (error) {
                console.log(`❌ Error with ${labelDef.name}: ${error.message}`);
              }
            }
            
            console.log(`🎉 Hub sync completed! Created: ${created}, Updated: ${updated}`);

  # Синхронизация в Satellites
  sync-satellite-labels:
    runs-on: ubuntu-latest
    needs: sync-hub-labels
    strategy:
      matrix:
        satellite: ['yandex-test', 'ozon-test', 'wildberries-test', 'wordpress', 'cdek-it.github.io', 'xstate-json-types', 'widget', 'sdk2.0', 'opencart', 'react-ui-kit', 'vue-ui-kit', 'yandex', 'ozon', 'wildberries', 'react-native-ui-kit', 'angular-ui-kit']
    
    steps:
      - name: Checkout Hub
        uses: actions/checkout@v4
        
      - name: Sync labels to ${{ matrix.satellite }}
        uses: actions/github-script@v7
        env:
          HUB_PAT: ${{ secrets.ADD_TO_PROJECT_PAT }}
        with:
          script: |
            const fs = require('fs');
            
            if (!fs.existsSync('./.github/labels-config.json')) {
              console.log('❌ labels-config.json not found');
              return;
            }
            
            const config = JSON.parse(fs.readFileSync('./.github/labels-config.json', 'utf8'));
            
            console.log(`🔄 Syncing labels to ${{ matrix.satellite }}...`);
            
            // Получаем все метки из конфига
            const allLabels = [];
            for (const category of Object.values(config.labels)) {
              if (Array.isArray(category)) {
                allLabels.push(...category);
              }
            }
            
            console.log(`📋 Processing ${allLabels.length} labels for ${{ matrix.satellite }}...`);
            
            try {
              // Используем actions/github-script с кастомным токеном
              // Создаем кастомный запрос с PAT токеном
              const response = await fetch(`https://api.github.com/repos/${context.repo.owner}/${{ matrix.satellite }}/labels`, {
                method: 'GET',
                headers: {
                  'Authorization': `token ${process.env.HUB_PAT}`,
                  'Accept': 'application/vnd.github.v3+json',
                  'User-Agent': 'GitHub-Label-Sync'
                }
              });
              
              if (!response.ok) {
                throw new Error(`HTTP ${response.status}: ${response.statusText}`);
              }
              
              const existingLabels = await response.json();
              const existingLabelNames = existingLabels.map(label => label.name);
              
              let created = 0;
              let updated = 0;
              
              // Создаем или обновляем метки
              for (const labelDef of allLabels) {
                try {
                  const labelData = {
                    name: labelDef.name,
                    color: labelDef.color,
                    description: labelDef.description || ''
                  };
                  
                  if (existingLabelNames.includes(labelDef.name)) {
                    // Обновляем существующую метку
                    const updateResponse = await fetch(`https://api.github.com/repos/${context.repo.owner}/${{ matrix.satellite }}/labels/${encodeURIComponent(labelDef.name)}`, {
                      method: 'PATCH',
                      headers: {
                        'Authorization': `token ${process.env.HUB_PAT}`,
                        'Accept': 'application/vnd.github.v3+json',
                        'Content-Type': 'application/json'
                      },
                      body: JSON.stringify(labelData)
                    });
                    
                    if (updateResponse.ok) {
                      updated++;
                      console.log(`🔁 Updated in ${{ matrix.satellite }}: ${labelDef.name}`);
                    } else {
                      throw new Error(`HTTP ${updateResponse.status}`);
                    }
                  } else {
                    // Создаем новую метку
                    const createResponse = await fetch(`https://api.github.com/repos/${context.repo.owner}/${{ matrix.satellite }}/labels`, {
                      method: 'POST',
                      headers: {
                        'Authorization': `token ${process.env.HUB_PAT}`,
                        'Accept': 'application/vnd.github.v3+json',
                        'Content-Type': 'application/json'
                      },
                      body: JSON.stringify(labelData)
                    });
                    
                    if (createResponse.ok) {
                      created++;
                      console.log(`✅ Created in ${{ matrix.satellite }}: ${labelDef.name}`);
                    } else {
                      throw new Error(`HTTP ${createResponse.status}`);
                    }
                  }
                } catch (error) {
                  console.log(`❌ Error with ${labelDef.name} in ${{ matrix.satellite }}: ${error.message}`);
                }
              }
              
              console.log(`🎉 ${{ matrix.satellite }} sync completed! Created: ${created}, Updated: ${updated}`);
              
            } catch (error) {
              console.log(`💥 Failed to sync ${{ matrix.satellite }}: ${error.message}`);
            }
