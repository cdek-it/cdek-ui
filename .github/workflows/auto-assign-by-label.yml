name: Auto-assign by label

on:
  issues:
    types: [opened, labeled]
  pull_request:
    types: [opened, labeled]

permissions:
  contents: read
  issues: write
  pull-requests: write

jobs:
  assign:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.ADD_TO_PROJECT_PAT }}
          fetch-depth: 1

      - name: Auto-assign team members based on label
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.ADD_TO_PROJECT_PAT }}
          script: |
            const fs = require("fs");
            const path = require("path");

            const labelsPath = path.join(process.env.GITHUB_WORKSPACE, ".github", "labels-config.json");
            if (!fs.existsSync(labelsPath)) {
              console.log("❌ labels-config.json не найден");
              return;
            }

            const configData = JSON.parse(fs.readFileSync(labelsPath, "utf8"));

            const labelToTeam = {
              "area/design-system": "design-team",
              "area/components": "frontend-specialists",
              "area/accessibility": "a11y-experts",
              "area/performance": "performance-team",
              "area/tooling": "devops-team",
              "area/i18n": "i18n-team",
              "area/mobile": "mobile-team"
            };

            const org = context.repo.owner;
            const item = context.payload.issue || context.payload.pull_request;
            if (!item) {
              console.log("❌ Не найден issue или PR");
              return;
            }

            const number = item.number;
            const labels = item.labels.map(l => l.name);
            console.log("🏷️ Лейблы:", labels);

            for (const label of labels) {
              const team = labelToTeam[label];
              if (!team) continue;

              try {
                // Получаем всех участников команды
                const members = await github.rest.teams.listMembersInOrg({
                  org,
                  team_slug: team,
                });

                const usernames = members.data.map(m => m.login);
                console.log(`👥 Назначаем команду ${team}: ${usernames.join(", ")}`);

                await github.rest.issues.addAssignees({
                  owner: org,
                  repo: context.repo.repo,
                  issue_number: number,
                  assignees: usernames,
                });
              } catch (error) {
                console.log(`⚠️ Ошибка при назначении для ${team}: ${error.message}`);
              }
            }

