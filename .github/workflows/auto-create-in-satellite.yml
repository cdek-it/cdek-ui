name: 🚀 Auto Create in Satellite

on:
  issues:
    types: [labeled]

jobs:
  auto-create-satellite-issue:
    runs-on: ubuntu-latest
    if: startsWith(github.event.label.name, 'satellite/')
    
    steps:
      - name: Checkout Hub
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm install @octokit/rest

      - name: Auto create issue in satellite
        uses: actions/github-script@v7
        with:
          script: |
            const { issue, label } = context.payload;
            
            if (!label.name.startsWith('satellite/')) return;
            
            const satelliteName = label.name.replace('satellite/', '');
            console.log('🔄 Auto-creating issue in satellite: ' + satelliteName);
            
            const hasRoutedLabel = issue.labels.some(l => l.name === 'status/routed');
            if (hasRoutedLabel) {
              console.log('ℹ️ Issue already routed to satellite, skipping');
              return;
            }
            
            await createSatelliteIssue(issue, satelliteName);
            
            async function createSatelliteIssue(hubIssue, satelliteName) {
              try {
                const repoMap = {
                  'yandex-test': 'yandex-test',
                  'ozon-test': 'ozon-test',
                  'wildberries-test': 'wildberries-test'
                };
                
                const targetRepo = repoMap[satelliteName];
                if (!targetRepo) {
                  console.log('❌ Unknown satellite: ' + satelliteName);
                  return;
                }
                
                // Используем встроенный github из контекста вместо octokit
                const satelliteIssue = await github.rest.issues.create({
                  owner: context.repo.owner,
                  repo: targetRepo,
                  title: '[From Hub] ' + hubIssue.title,
                  body: '## 📡 Задача создана из Hub\n\n' +
                        '**Original Issue**: #' + hubIssue.number + '\n' +
                        '**Hub URL**: ' + hubIssue.html_url + '\n' +
                        '**Created By**: @' + hubIssue.user.login + '\n' +
                        '**Satellite**: ' + satelliteName + '\n' +
                        '**Auto-created**: ' + new Date().toISOString() + '\n\n' +
                        '---\n\n' +
                        hubIssue.body + '\n\n' +
                        '---\n\n' +
                        '### 🔗 Связь с Hub\n' +
                        '- Все обсуждения и планирование в [Hub Issue #' + hubIssue.number + '](' + hubIssue.html_url + ')\n' +
                        '- Код и реализация в этом репозитории\n' +
                        '- При создании PR ссылайтесь на эту задачу\n' +
                        '- Статус синхронизируется автоматически',
                  labels: ['from-hub', 'needs-triage']
                });
                
                await github.rest.issues.removeLabel({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  issue_number: hubIssue.number,
                  name: 'status/needs-routing'
                });
                
                await github.rest.issues.addLabels({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  issue_number: hubIssue.number,
                  labels: ['status/routed', 'auto-created']
                });
                
                await github.rest.issues.createComment({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  issue_number: hubIssue.number,
                  body: '✅ **Задача автоматически создана в satellite!**\n\n' +
                        '**Satellite**: `' + targetRepo + '`\n' +
                        '**Issue**: [#' + satelliteIssue.data.number + '](' + satelliteIssue.data.html_url + ')\n' +
                        '**Auto-created**: ' + new Date().toLocaleString() + '\n\n' +
                        '**Статус**: 🟢 Активно в satellite'
                });
                
                console.log('✅ Created issue in ' + targetRepo + ': #' + satelliteIssue.data.number);
                
              } catch (error) {
                console.log('❌ Failed to create in ' + satelliteName + ': ' + error.message);
                
                await github.rest.issues.createComment({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  issue_number: hubIssue.number,
                  body: '❌ **Ошибка автосоздания в satellite**\n\n' +
                        'Не удалось создать задачу в satellite `' + satelliteName + '`:\n' +
                        '```\n' + error.message + '\n```'
                });
              }
            }
