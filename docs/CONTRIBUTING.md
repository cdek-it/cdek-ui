# Руководство по внесению вклада

Руководство поможет вам правильно внести свой вклад в развитие дизайн-системы СДЭК.

## Процесс работы

### 1. Обсуждение и создание задачи в Hub

Выберите подходящий тип задачи:

- ⚙️ [Общая задача](https://github.com/cdek-it/cdek-ui/issues/new?template=issue-general.yaml) – улучшения основного функционала
- 💡 [Новый функционал](https://github.com/cdek-it/cdek-ui/issues/new?template=issue-creation.yaml) – новые компоненты или функциональность
- 🐞 [Отчёт об ошибке](https://github.com/cdek-it/cdek-ui/issues/new?template=issue-bug.yaml) – исправление багов
- 💬 [Вопросы и идеи](https://github.com/cdek-it/cdek-ui/discussions) – обсуждение концепций

### 2. Маршрутизация в Satellite

После создания задачи в Hub:

1. Добавьте лейбл `satellite/имя-репозитория`:
   - `satellite/angular-ui-kit`
   - `satellite/vue-ui-kit`
   - `satellite/react-ui-kit`
   - `satellite/react-native-ui-kit`

2. Система автоматически:
   - Создаст задачу в satellite-репозитории
   - Добавит ссылку на satellite-задачу в Hub
   - Установит лейбл `state/routed`

3. Дождитесь одобрения от мейнтейнеров (лейбл `status/ready-for-dev`)

### 3. Разработка в Satellite

**Подготовка окружения:**
- Клонируйте satellite-репозиторий
- Установите зависимости
- Создайте ветку от main

**Именование веток:**
- `feature/issue-123-description` – новая функциональность
- `fix/issue-123-description` – исправление бага
- `refactor/issue-123-description` – рефакторинг
- `docs/issue-123-description` – документация

**Во время разработки:**
- Обновляйте статус задачи (добавьте лейбл `status/in-progress`)
- Следуйте code-style проекта
- Пишите тесты для нового кода

### 4. Code Review и Pull Request

**Перед созданием PR:**
- Убедитесь, что код соответствует стандартам
- Запустите тесты
- Проверьте сборку

**Создание Pull Request:**
1. Создайте PR в satellite-репозитории
2. Используйте шаблон описания PR:

```markdown
## Описание
Краткое описание изменений

## Связанные задачи
Closes #123 (Hub issue)
Closes cdek-it/[satellite]#456 (Satellite issue)

## Тип изменений
- [ ] Новая функциональность
- [ ] Исправление бага
- [ ] Рефакторинг
- [ ] Документация
- [ ] Тесты
```

3. Добавьте лейбл `status/needs-review` в задачу
4. Запросите review у мейнтейнеров
**Процесс review:**
- Отвечайте на комментарии ревьюеров
- Вносите запрошенные изменения
- Обновляйте PR при необходимости
- Не форсируйте мерж без апрува

### 5. Мерж и завершение

После получения approval:
1. Мейнтейнер выполнит мерж PR
2. Задача в satellite автоматически закроется
3. Задача в Hub также закроется автоматически
4. Изменения попадут в следующий релиз

## Система лейблов

Используйте структурированную систему лейблов для организации работы:

**Status (статус задачи):**
- `status/needs-triage` – требует review
- `status/ready-for-dev` – готово к разработке
- `status/in-progress` – в разработке
- `status/needs-review` – ожидает code review
- `status/blocked` – заблокировано

**Priority (приоритет):**
- `priority/critical` – критический
- `priority/high` – высокий
- `priority/medium` – средний
- `priority/low` – низкий

**Type (тип задачи):**
- `type/bug` – исправление багов
- `type/feature` – новая функциональность
- `type/enhancement` – улучшение существующего
- `type/refactor` – рефакторинг
- `type/documentation` – документация
- `type/testing` – тесты

**Area (область):**
- `area/design-system` – core дизайн-система
- `area/components` – UI компоненты
- `area/accessibility` – a11y
- `area/performance` – производительность
- `area/tooling` – developer experience

## Автоматическая синхронизация

Система автоматически синхронизирует:

**Hub → Satellite:**
- Создание задачи при добавлении `satellite/` лейбла
- Лейблы: status/, priority/, area/, type/
- Комментарии пользователей

**Satellite → Hub:**
- Статус задачи (открыта/закрыта)
- Лейблы: status/, priority/, area/, type/
- Комментарии пользователей

**Не синхронизируются:**
- Системные лейблы: `satellite/`, `state/routed`, `state/from-hub`
- Комментарии ботов и системные сообщения

## Коммуникация

### Где задавать вопросы

- **GitHub Discussions** – общие вопросы, идеи, обсуждения
- **Issue комментарии** – вопросы по конкретной задаче

Спасибо за ваш вклад! 🚀
